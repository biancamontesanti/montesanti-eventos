export interface Event {
    title: string;
    description: string;
  }
  
  export interface Testimonial {
    name: string;
    role: string;
    text: string;
  }
  
  export interface Service {
    title: string;
    description: string;
  }

  export interface Video {
    id: number;
    url: string;
    title: string;
    thumbnail: string;
  }